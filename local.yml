---
# Default playbook for ansible-pull, which runs local.yml from the repository
# root unless told otherwise.
#
# hosts: all rather than a group name — ansible-pull generates its own inventory
# containing just the machine it is running on, so a group from inventory.ini
# would not match.
- name: Bootstrap host
  hosts: all
  become: false
  roles:
    - bootstrap
