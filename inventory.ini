# Every playbook in this repo configures the machine it runs on.
# ansible_connection=local is required: an explicitly defined localhost does
# not get the local connection automatically the way an implicit one does.
[local]
localhost ansible_connection=local ansible_python_interpreter="{{ ansible_playbook_python }}"
